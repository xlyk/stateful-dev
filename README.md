# stateful-dev

Stateful development worker helper tooling for Hermes cron agents.

This repository will provide a small Python CLI and Hermes plugin wrapper that make stateful cron development safer: plan parsing, durable state validation, atomic locking, legal state transitions, evidence capture, and compact reports.
